class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        int n=nums1.size();
        //vector<int>nums2(n);

        // for(int i=0; i<n; i++){
        //     if(nums[i]%2==0)
        // }
        // for(int i=0; i<n; i++){
        //     if(nums[i]%2 !=0)
        // }
        //or any odd value with even value then aray become true;
        return true;
    }
};